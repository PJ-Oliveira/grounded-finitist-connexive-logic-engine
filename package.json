{
  "name": "grounded-finitist-connexive-logic-engine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=docs/bundle.js --platform=browser && copyfiles -u 1 index.html style.css docs",
    "start": "npm run build && serve deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
