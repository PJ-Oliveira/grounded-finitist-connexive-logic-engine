{
  "name": "grounded-finitist-connexive-logic-engine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "clean": "rm -rf deploy",
  "build": "npm run clean && esbuild src/main.ts --bundle --outfile=deploy/bundle.js && cp index.html style.css config.json deploy/",
  "start": "npm run build && serve deploy",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
